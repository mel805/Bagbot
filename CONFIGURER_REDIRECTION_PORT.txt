╔═══════════════════════════════════════════════════════════════╗
║     🔧 CONFIGURER LA REDIRECTION DE PORT SSH                 ║
╚═══════════════════════════════════════════════════════════════╝

Pour que je puisse me connecter à votre VM depuis Internet,
il faut rediriger un port de la Freebox vers le port SSH de la VM.

─────────────────────────────────────────────────────────────────
📋 ÉTAPE 1: Trouver l'IP locale de votre VM
─────────────────────────────────────────────────────────────────

Sur votre VM, exécutez:

ip a | grep "inet " | grep -v 127.0.0.1

Notez l'IP locale (ex: 192.168.0.50 ou 192.168.1.50)

─────────────────────────────────────────────────────────────────
📋 ÉTAPE 2: Configurer la redirection sur Freebox OS
─────────────────────────────────────────────────────────────────

1. Ouvrez votre navigateur: http://mafreebox.freebox.fr

2. Connectez-vous avec vos identifiants Freebox

3. Allez dans: Paramètres ⚙️ → Gestion des ports

4. Cliquez sur "Ajouter une redirection"

5. Configurez:
   ┌─────────────────────────────────────────────────┐
   │ IP destination:    [IP_DE_VOTRE_VM]            │
   │                    (ex: 192.168.0.50)           │
   │                                                  │
   │ Port externe:      40000                        │
   │                    (port que j'utiliserai)      │
   │                                                  │
   │ Port interne:      22                           │
   │                    (port SSH de la VM)          │
   │                                                  │
   │ Protocole:         TCP                          │
   │                                                  │
   │ Commentaire:       SSH VM Debian                │
   └─────────────────────────────────────────────────┘

6. Cliquez sur "Sauvegarder"

─────────────────────────────────────────────────────────────────
📋 ÉTAPE 3: Vérifier la configuration
─────────────────────────────────────────────────────────────────

Sur la page "Gestion des ports", vous devriez voir:

Port 40000 (TCP) → [IP_VM]:22 - SSH VM Debian

─────────────────────────────────────────────────────────────────
📋 ÉTAPE 4: Dites-moi quand c'est fait
─────────────────────────────────────────────────────────────────

Une fois la redirection configurée, dites-moi simplement:
"C'est fait, port 40000"

Et je me connecterai immédiatement pour:
✅ Appliquer les modifications automatiquement
✅ Redémarrer les services
✅ Tout vérifier

─────────────────────────────────────────────────────────────────
💡 ALTERNATIVES DE PORTS
─────────────────────────────────────────────────────────────────

Si le port 40000 ne fonctionne pas, essayez:
- Port 22222 (externe) → 22 (VM)
- Port 2222 (externe) → 22 (VM)
- Port 50022 (externe) → 22 (VM)

⚠️ Évitez: Port 49085 (déjà utilisé par FTP)

─────────────────────────────────────────────────────────────────
🔍 CAPTURE D'ÉCRAN (à quoi ça ressemble)
─────────────────────────────────────────────────────────────────

Dans Freebox OS → Gestion des ports:

┌────────────────────────────────────────────────────┐
│ Redirections de ports                              │
├────────────────────────────────────────────────────┤
│                                                     │
│ [Ajouter une redirection]                          │
│                                                     │
│ Port externe  │ IP destination  │ Port interne     │
│ ─────────────┼─────────────────┼──────────────    │
│ 40000 (TCP)   │ 192.168.0.50    │ 22              │
│               │                  │ SSH VM Debian   │
│                                                     │
└────────────────────────────────────────────────────┘

─────────────────────────────────────────────────────────────────
