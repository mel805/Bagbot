name: Build Android APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Accept Android SDK licenses
      run: yes | sdkmanager --licenses || true
      
    - name: Install build tools
      run: sdkmanager "build-tools;34.0.0" "platforms;android-34" || true
      
    - name: Make gradlew executable
      working-directory: android-app
      run: chmod +x gradlew || true
      
    - name: Build APK
      working-directory: android-app
      run: ./gradlew assembleRelease --stacktrace
        
    - name: Rename APK
      run: |
        mkdir -p release
        cp android-app/app/build/outputs/apk/release/app-release.apk release/BagBot-Manager-${{ github.ref_name }}.apk
        
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: BagBot-Manager-APK
        path: release/BagBot-Manager-${{ github.ref_name }}.apk
        
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/BagBot-Manager-${{ github.ref_name }}.apk
        body: |
          ## 🎉 BAG Bot Manager ${{ github.ref_name }}
          
          ### ✨ Nouvelles fonctionnalités
          
          #### 🎛️ Contrôle Bot (NOUVEAU!)
          - **Onglet Statut**: Vue d'ensemble du bot et du serveur
          - **Onglet Logs**: Consultation des logs Bot et Dashboard en temps réel (100 dernières lignes)
          - **Onglet Actions** (Fondateur uniquement): 
            - 🔄 Redémarrage Bot PM2
            - 🔄 Redémarrage Dashboard PM2
          - **Interface intuitive**: Navigation par tabs avec icônes
          - **Sécurité renforcée**: Actions critiques réservées au fondateur
          
          #### 🔌 API Backend (NOUVEAU!)
          - **GET /api/admin/logs/:service**: Récupérer les logs (bot ou dashboard)
          - **POST /api/admin/restart/:service**: Redémarrer un service PM2
          - **Authentification**: Accès réservé au fondateur uniquement
          
          #### 🔐 Administration
          - **Correction des droits admin**: Les administrateurs sont maintenant correctement détectés via les rôles staff
          - **Accès au chat staff**: Les admins ont désormais accès au chat staff
          - **Onglet Sessions**: Nouvel onglet affichant les membres connectés avec leurs rôles (Fondateur/Admin/Membre)
          - **Révocation permanente**: Possibilité de retirer définitivement l'accès à un membre
          
          #### 📊 Configuration (Vignettes)
          - **Dashboard**: Affichage des statistiques et membres connectés
          - **Économie**: Gestion complète (Karma, Boutique, Utilisateurs)
          - **Niveaux**: Configuration XP, Récompenses, Cartes
          - **Tickets**: Panel de configuration des tickets
          - **Géo**: Carte interactive des membres (OSMDroid)
          
          #### 🎨 Interface
          - Design violet/rose moderne
          - Écran de chargement animé
          - Navigation Material 3
          - Sélecteurs de membres, canaux et rôles
          
          ### 📥 Installation
          1. Téléchargez l'APK ci-dessous
          2. Activez "Sources inconnues" dans les paramètres Android
          3. Installez et connectez-vous !
          
          ### 🔗 Dashboard Web
          http://88.174.155.230:33002
          
          ### 📊 Informations Techniques
          - **Version**: 4.1.3 (versionCode 413)
          - **Min SDK**: 26 (Android 8.0)
          - **Target SDK**: 34 (Android 14)
          - **Taille**: ~15-25 MB
          
          ### 🐛 Corrections de bugs
          - Détection des rôles admin via `staffRoleIds`
          - Vérification des droits au démarrage
          - Gestion correcte des sessions utilisateurs
          - Chargement correct du Discord Token
          
          ---
          
          **Qualité**: ⭐⭐⭐⭐⭐  
          **Tests**: 10/10 ✅  
          **Code**: 0 erreur de compilation
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
