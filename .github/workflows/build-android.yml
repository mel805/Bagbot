name: Build Android APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Accept Android SDK licenses
      run: yes | sdkmanager --licenses || true
      
    - name: Install build tools
      run: sdkmanager "build-tools;34.0.0" "platforms;android-34" || true
      
    - name: Make gradlew executable
      working-directory: android-app
      run: chmod +x gradlew || true
      
    - name: Build APK
      working-directory: android-app
      run: ./gradlew assembleRelease --stacktrace
        
    - name: Rename APK
      run: |
        mkdir -p release
        cp android-app/app/build/outputs/apk/release/app-release.apk release/BagBot-Manager-${{ github.ref_name }}.apk
        
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: BagBot-Manager-APK
        path: release/BagBot-Manager-${{ github.ref_name }}.apk
        
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/BagBot-Manager-${{ github.ref_name }}.apk
        body: |
          ## 🎉 BAG Bot Manager ${{ github.ref_name }} - Synchronisation complète avec le Dashboard
          
          ### ✨ Nouvelles catégories de configuration (9 ajoutées !)
          
          #### Modération & Sécurité
          - 📝 **Logs** - Configuration complète par catégorie avec sélecteurs de channels
          - 👥 **Rôles Staff** - Gestion des rôles avec interface moderne
          - 👢 **AutoKick** - Kick automatique avec suivi des membres
          - ⏰ **Kick Inactivité** - Gestion de l'inactivité avec rôles exempts
          
          #### Fonctionnalités
          - 💬 **Confessions** - Mode SFW/NSFW avec prompts personnalisés
          - 🧵 **AutoThread** - Création automatique de threads avec modes de nommage
          - 📢 **Disboard** - Rappels automatiques pour le bump
          - 🔢 **Comptage** - Gestion des channels de comptage
          - 🎲 **Action ou Vérité** - Configuration complète SFW/NSFW
          
          ### 🎨 Améliorations de l'interface
          - ✅ **Vrais sélecteurs** de rôles, channels et membres (plus de JSON !)
          - ✅ **Messages configurables** avec variables ({user}, {guild}, etc.)
          - ✅ **Embeds personnalisables** (titre, description, couleur, footer)
          - ✅ **Onglets SFW/NSFW** pour les catégories appropriées
          - ✅ **Listes dynamiques** avec ajout/suppression intuitive
          - ✅ **Compteurs et statistiques** en temps réel
          
          ### 📊 Résultat
          L'application Android a maintenant **exactement les mêmes catégories** que le dashboard web !
          
          ### 📥 Installation
          1. Téléchargez l'APK ci-dessous
          2. Activez "Sources inconnues" dans les paramètres
          3. Installez et profitez de toutes les fonctionnalités !
          
          ### 🔗 Serveur: http://88.174.155.230:33002
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
