# Discord Bot Token
DISCORD_TOKEN=votre_token_discord_ici
CLIENT_ID=votre_client_id_ici

# Configuration
DATA_DIR=/home/bagbot/Bag-bot/data
USE_PG=false

# GitHub (optionnel - pour sauvegardes)
GITHUB_TOKEN=
GITHUB_REPO=

# Logs Channel IDs (optionnel)
LOGS_CHANNEL_ID=
MOD_LOGS_CHANNEL_ID=
LEVEL_CARD_LOGO_URL="https://cdn.discordapp.com/attachments/1408458115283812484/1411752143173714040/IMG_20250831_183646.png?ex=68b5cc24&is=68b47aa4&hm=1ef2d1f822c69a749023e5ca003124e619ff91c922e1299c3fb672647deb1242&"

# ===== Dashboard (gestion via Freebox / Web) =====
# Protéger le dashboard (Bearer token). Si vide, les routes d'écriture restent ouvertes (déconseillé).
DASHBOARD_ADMIN_TOKEN=

# Connexion via Discord (OAuth2) - requis pour "seuls les admins du serveur"
# DISCORD_OAUTH_CLIENT_ID peut réutiliser CLIENT_ID si c'est le même.
DISCORD_OAUTH_CLIENT_ID=
# Secret OAuth2 (à créer sur Discord Developer Portal). Peut aussi être stocké dans dashboard-secrets.json:
# { "discordOAuthClientSecret": "..." }
DISCORD_OAUTH_CLIENT_SECRET=
# Optionnel: si vide, le redirect URI est auto-calculé depuis l'URL actuelle.
DISCORD_OAUTH_REDIRECT_URI=

# Webhook signé (HMAC SHA256) pour piloter à distance.
# Exemple: POST /webhook/restart avec header X-Signature: sha256=<hex>
DASHBOARD_WEBHOOK_SECRET=

# Dashboard port (si différent)
DASHBOARD_PORT=33002

# Chemins/stockage (optionnel)
# BAGBOT_CONFIG_PATH=/home/bagbot/Bag-bot/data/config.json
# BAGBOT_BACKUP_DIR=/var/data/backups

# Fichier secrets auto-généré (clé cookie + webhook, etc.)
# DASHBOARD_SECRETS_PATH=/home/bagbot/Bag-bot/data/dashboard-secrets.json
